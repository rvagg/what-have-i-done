<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' | ' : '' %>What have I done?</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/styles.css">
  <%- defineContent('styles') %>
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/"><i class="bi bi-github me-2"></i>What have I done?</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/activity">Generate Report</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/reports"><i class="bi bi-journal-text me-1"></i>Saved Reports</a>
          </li>
        </ul>
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/settings"><i class="bi bi-gear me-1"></i>Settings</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <% if (locals.flashMessages) { %>
      <% if (locals.flashMessages.success) { %>
        <div class="alert alert-success">
          <%= locals.flashMessages.success %>
        </div>
      <% } %>
      <% if (locals.flashMessages.error) { %>
        <div class="alert alert-danger">
          <%= locals.flashMessages.error %>
        </div>
      <% } %>
      <% if (locals.flashMessages.warning) { %>
        <div class="alert alert-warning">
          <%= locals.flashMessages.warning %>
        </div>
      <% } %>
    <% } %>

    <%- body %>
  </div>

  <footer class="footer mt-5 py-3 bg-dark text-light"></footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
  <%- defineContent('scripts') %>
</body>
</html>